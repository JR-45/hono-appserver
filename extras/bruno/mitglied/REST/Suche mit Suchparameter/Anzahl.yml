info:
  name: Anzahl
  type: http
  seq: 3

http:
  method: GET
  url: "{{restUrl}}?count-only="
  headers:
    - name: Accept
      value: application/json
  params:
    - name: count-only
      value: ""
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.count
      operator: isNumber

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
